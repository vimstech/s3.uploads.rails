<%= form_for User.new do |f| %>
  <%= f.file_field :photo, required: true %>
  <%= f.submit 'Upload', :name => 'upload',class: 'button' %>
<% end %>
<ul>
  <% @users.each do |user|%>
  <li><%=image_tag(user.photo_url, size: 100)%></li>
  <% end %>
</ul>